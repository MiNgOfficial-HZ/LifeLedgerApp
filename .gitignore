# 构建中间产物
work/

# 本机工具链路径配置（模板见 build.config.example.ps1）
build.config.ps1

# 构建脚本的输出目录（CI 与本地 -OutDir dist 都会用到）
dist/

# 签名密钥（由 build.ps1 首次构建时自动生成，不要提交）
*.keystore
*.jks

# 编译输出
*.class
gen/

# 打包产物
*.apk
*.idsig
*.zip

# 编辑器 / 系统
.idea/
.vscode/
*.iml
Thumbs.db
desktop.ini
